'use client'

import Section from '@/components/General/Section'

type Props = {}

function FirstSection({}: Props) {
  return (
    <Section className='section-first'>
      <h1 className='text-6xl'>FirstSection</h1>
    </Section>
  )
}

export default FirstSection
